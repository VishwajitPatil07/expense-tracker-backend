# .env

# PostgreSQL database connection string
DATABASE_URL=postgresql://expenseuser:spendyzer@localhost:5432/expensetracker

# Optional: Node environment setting
NODE_ENV=development

# Optional: Server port (if your app uses this)
PORT=3000
